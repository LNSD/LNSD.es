box: wercker/nodejs
build:
  steps:
    - npm-install
    - grunt:
        tasks: build
deploy:
  steps:
    - gh-pages:
        token: $GH_TOKEN
        domain: lnsd.es
        basedir: build