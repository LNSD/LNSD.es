box: wercker/nodejs
build:
  steps:
    - npm-install
    - plasticine/bower-install
    - grunt:
        fail-on-warnings: true
        tasks: build
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: lnsd.es
        basedir: build